---
layout: default
class: text-2xl
---

# When does it run the callback?

```ts
function loop() {
  setTimeout(() => {
    loop();
  });
}

function loop2() {
  Promise.resolve().then(() => {
    loop2();
  });
}

loop();
loop2();
```
